<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="android:textColorPrimary">#ffffff</item>-->
        <item name="android:textColorHint">@color/colorPrimaryDark</item>
    </style>

    <style name="ActionBarThemeOverlay" parent="">
        <item name="android:textColorPrimary">#fff</item>
        <item name="colorControlNormal">#fff</item>
        <item name="colorControlHighlight">#3fff</item>
    </style>

    <style name="HeaderBar">
        <item name="android:background">?colorPrimary</item>
    </style>

    <style name="ActionBarPopupThemeOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:background">@android:color/white</item>
        <item name="android:textColor">#000</item>
    </style>

    <style name="ToolbarButton" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">@dimen/default_icon_size</item>
        <item name="android:layout_height">@dimen/default_icon_size</item>
    </style>

    <style name="ListItemWeather">
        <item name="android:textSize">20sp</item>
    </style>

    <!--New-->
    <style name="PrintText">
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:shadowColor">@color/mask</item>
        <item name="android:shadowRadius">25</item>
    </style>

    <style name="PrintTextDark">
        <item name="android:textColor">@color/colorDarkGrey</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="PrintTextGrey">
        <item name="android:textColor">@color/colorGrey</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>

    <!--new-->

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.Button" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">@color/colorPrimaryDark</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>

</resources>
